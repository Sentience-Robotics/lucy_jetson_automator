---
# Ansible Galaxy requirements file

# Collections
collections:
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"

# External roles (if needed in the future)
roles: [] 