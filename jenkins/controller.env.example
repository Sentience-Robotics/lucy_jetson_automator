# Copy to controller.env (gitignored) on the VPS — do not commit secrets.
# Git remote for this automation repo (HTTPS or SSH URL Jenkins can reach).
CONTROLLER_REPO_URL=https://github.com/your-org/lucy_jetson_automator.git

# CasC location.url (operators often use VPN hostname + HTTPS reverse proxy).
# JENKINS_URL=https://jenkins.vpn.example/
